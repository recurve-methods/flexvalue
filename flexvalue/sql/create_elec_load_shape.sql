CREATE TABLE elec_load_shape (
    state VARCHAR,
    utility VARCHAR,
    region VARCHAR,
    quarter VARCHAR,
    month INTEGER,
    hour_of_day INTEGER,
    hour_of_year INTEGER,
    load_shape_name VARCHAR,
    value FLOAT
);
